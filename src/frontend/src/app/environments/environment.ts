export const environment = {
  production: false,
  // Giá trị này chỉ là giữ chỗ (placeholder). 
  // Khi chạy lệnh 'npm start', script sẽ ghi đè giá trị thật từ .env vào đây.
  apiUrl: 'http://localhost:5000' 
};